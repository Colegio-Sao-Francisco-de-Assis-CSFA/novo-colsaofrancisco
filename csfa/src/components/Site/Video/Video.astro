---
interface Props {
    src: string;
}

const{src} = Astro.props;

---

<video class="aspect-video max-h-80 rounded-md" autoplay muted controls>
    <source src={src} type="webp">
    Your browser does not support the video tag.
</video>

